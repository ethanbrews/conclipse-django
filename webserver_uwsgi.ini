[uwsgi]

chdir           = /home/webserver/webserver
module          = webserver.wsgi
home            = /home/webserver/venv

master          = true
processes       = 10
socket          = /home/webserver/webserver/webserver.sock
chmod-socket    = 664
vacuum          = true

uid             = webserver
gid             = webserver
